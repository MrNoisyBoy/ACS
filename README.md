# ACS
# 🚀 Система управления доступом (ACS)

Система контроля доступа к файлам с ролевой моделью и MD5 аутентификацией.

## 📊 Учетные данные

| № | Логин | Пароль | Роль | MD5 Хэш |
|---|---|---|---|---|
| 1 | sysadmin | sysadmin | Системный администратор | 48a365b4ce1e322a55ae9017f3daf0c0 |
| 2 | admin | admin | Администратор | 21232f297a57a5a743894a0e4a801fc3 |
| 3 | manager | manager | Менеджер | 1d0258c2440a8d19e716292b231e3190 |
| 4 | designer | designer | Веб-дизайнер | 230ace927da4bb74817fa22adc663e0a |
| 5 | developer | developer | Разработчик | 5e8edd851d2fdfbd7415232c67367cc3 |
| 6 | analyst | analyst | Аналитик | 05d5c5dfb743a5bd8fd7494fdc9bdb00 |
| 7 | guest | guest | Гость | 084e0343a0486ff05530df6c705c8bb4 |

## 📁 Структура проекта

```
ACS_System/
├── main.py                    # Основная программа
├── users.json                 # База пользователей с MD5 хэшами
├── config.yaml               # Конфигурация системы
├── constants.py              # Текстовые константы
├── requirements.txt          # Зависимости
├── app.log                  # Лог работы системы
├── workspace/               # Рабочая директория с файлами
├── core/                    # Ядро системы
├── storage/                 # Хранилища данных
└── utils/                   # Вспомогательные утилиты
```

## 🚀 Запуск системы

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Запустите систему:**
```bash
python main.py
```

3. **Используйте логины и пароли из таблицы выше**

## ✨ Возможности

### 🔐 Аутентификация
- MD5 хэширование паролей
- 3 попытки входа
- Ролевая модель (7 ролей)

### 📁 Управление файлами
- Просмотр доступных файлов
- Чтение файлов
- Создание и редактирование
- Удаление файлов (с подтверждением)
- Организация по папкам

### 🛡️ Права доступа

**Системный администратор (sysadmin):**
- Полный доступ ко всем файлам
- Все операции: read, write, delete, list

**Администратор (admin):**
- Доступ к reports, backups, shared
- Все операции кроме системных файлов

**Менеджер (manager):**
- Чтение и запись в reports и shared

**Веб-дизайнер (designer):**
- Работа с design и shared

**Разработчик (developer):**
- Доступ к code, temp, shared

**Аналитик (analyst):**
- Только чтение reports и analytics

**Гость (guest):**
- Только просмотр shared

## 🛠️ Вспомогательные скрипты

### Проверка паролей:
```bash
python check_passwords.py
```

### Проверка структуры проекта:
```bash
python check_structure.py
```

### Создание недостающих файлов:
```bash
python fix_imports.py
```

## ⚙️ Настройка

### Конфигурация (config.yaml):
```yaml
system:
  workspace_root: "./workspace"  # Рабочая папка
  log_level: "INFO"             # Уровень логирования
  log_file: "app.log"          # Файл лога
```

### Добавление пользователей:
1. Отредактируйте `users.json`
2. Пароли хранятся как MD5 хэши
3. Для генерации хэша: `hashlib.md5("пароль".encode()).hexdigest()`

## 📝 Рабочий процесс

1. **Вход в систему** с логином и паролем
2. **Выбор действия** из меню:
   - Просмотр файлов
   - Чтение файлов
   - Создание/редактирование
   - Удаление файлов
   - Просмотр информации о системе
3. **Выход из системы** или завершение работы

## 🔧 Технические детали

- **Хранение паролей**: MD5 хэши (пароль = логин)
- **Файловая структура**: Автоматическое создание папок
- **Логирование**: Подробное логирование в app.log
- **Обработка ошибок**: Защита от ввода некорректных данных

## ⚠️ Важно

- Пароль всегда равен логину (например, `manager`/`manager`)
- Файлы создаются в папке `workspace/`
- Логи хранятся в `app.log`
- Не изменяйте `users.json` вручную, используйте вспомогательные скрипты

## 📞 Поддержка

При возникновении ошибок:
1. Проверьте логи в `app.log`
2. Запустите `python check_structure.py`
3. Убедитесь, что используете правильные учетные данные

---
**Система готова к работе! Используйте логин `manager` с паролем `manager` для тестирования.**
